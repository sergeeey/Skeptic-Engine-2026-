# H10 Mapped Datasets

This directory stores normalized benchmark-ready CSV outputs produced from the
raw `H10` route assets.

These files are derived artifacts and should be regenerated whenever the raw
route inputs change.
